// Which storage the app uses. This static copy is the installable phone app,
// which keeps everything on the device; server.js serves its own version of
// this file that switches the same app to the server's API.
window.KENNA_BACKEND = 'local';
